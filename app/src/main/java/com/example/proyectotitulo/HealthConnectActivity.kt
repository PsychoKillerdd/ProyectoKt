package com.example.proyectotitulo

import androidx.appcompat.app.AppCompatActivity

/**
 * This is a dummy, translucent activity that is required by the Health Connect SDK
 * to handle the permission request flow. It should not be used directly.
 * Its existence in the manifest is enough for the permission system to work.
 */
class HealthConnectActivity : AppCompatActivity() {
    // This activity's content is handled by the Health Connect SDK.
    // No further implementation is needed.
}
